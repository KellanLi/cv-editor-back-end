---
description: Project domain, stack, naming, and src/ layout for cv-editor-back-end
alwaysApply: true
---

# cv-editor-back-end — 全局上下文

## 项目背景

- **产品**：AI 辅助的简历编辑系统。
- **技术栈**：NestJS（Express）、pnpm、Prisma + MySQL、LangChain。

## 领域名词（中英 / 代码用语一致）

| 中文     | 英文 / 代码用语           |
| -------- | ------------------------- |
| 简历     | Resume                    |
| 模块     | Section                   |
| 模块内容 | Section Content / Content |
| 模块模版 | Content Template          |
| 信息层   | Info Layer                |

## 领域模型要点

- 简历由多个 **Section** 组成；每类经历/区块对应一种 **Section Template**（固定字段结构）。
- **Section Template** 由若干 **Info Layer** 拼接；用户可为每层字段配置显示名称（如「职位名称」「实习 base」）。
- 系统提供三种 **Info Layer**：左标题右时间段、左右文字、富文本。

## 实现约定

- API、DTO、Prisma 与代码命名优先与上表英文用语一致；讨论需求时以上述中英对照为准。
- 详细背景可与 `docs/project.md` 对照，二者冲突时以仓库内文档为准并同步更新。

## `src/` 目录结构（一级子目录）

- **`common/`**：跨模块复用 — 统一 DTO（分页、响应包装、按表/业务拆分的 DTO）、装饰器（如 API 响应包装、JWT payload）、错误码枚举、业务异常、全局 HTTP 异常过滤器、响应转换拦截器等。
- **`config/`**：应用配置加载（如 `loader.ts`），配合根模块 `ConfigModule`。
- **`generated/`**：Prisma Client 生成输出（`schema.prisma` 中 `output` 指向此处）。**禁止手改**；类型与客户端以 `prisma generate` 为准。
- **`modules/`**：Nest 功能模块 — `auth`、`user`、`resume`、`content-template`；典型含 `*.module.ts`、`*.controller.ts`、`*.service.ts`、`dto/`。
- **`provider/`**：全局基础设施 — `prisma`（数据库）、`jwt`（JWT 模块与 Guard），供各 feature 注入。
- **`types/`**：跨层 TypeScript 类型（请求上下文、响应形状、认证相关等）。

**根文件**：`main.ts` 入口；`app.module.ts` / `app.controller.ts` / `app.service.ts` 为应用根装配与根级占位。
