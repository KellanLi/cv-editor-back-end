# 2026-04-26 Task Log

## 04:38

### Cursor Prompt

将 `web_search` 从占位实现替换为真实 Search API（选用 Tavily）；补充环境变量与 LangGraph 注入；并记录修改、提交。另含近期 AI 对话、Prisma 与流式接口等相关工作一并纳入本次提交。

### 修改内容总结

- **联网检索（Tavily）**：`web-search.tool.ts` 使用 `POST https://api.tavily.com/search` + `Authorization: Bearer`；`langgraph.config.ts` 增加 `getTavilySearchApiKey`（`TAVILY_API_KEY`）；`buildAgentTools` / `LanggraphService` 注入 Key；未配置 Key 时工具返回说明、不伪造结果。
- **系统提示（JD）**：`basic-qa.system.prompt.ts` 中 JD 每轮默认写入 system，与 `load_resume_context` 分工明确，未工具化 job_description。
- **AI 模块与数据模型（与本次同批变更）**：Prisma 迁移 `ai_conversation` / `ai_message` / `ai_global_context` / `ai_tool_call` 等；`src/modules/ai` 提供对话 CRUD、全局上下文、**`POST /ai/chat/stream`（SSE）** 与 `LangGraph` 流式；`src/provider/langgraph` 中 `createReactAgent`、`streamBasicQa` 与 `load_resume_context` 等工具；`docs/ai-design.md` 与项目规则小更新等。
- 前端 `POST` + `text/event-stream` 的调用方式已在对话中说明：需 `fetch` 读流、JWT `Authorization`；`EventSource` 不适用带 Body 的 POST。

## 21:54

### Cursor Prompt

排查 LLM 未调用 `web_search` 的原因；查询解决方案；可行则修复。随后要求记录修改并提交。

### 修改内容总结

- **根因说明（产品/请求）**：`enableWebSearch` 默认不挂 `web_search`；未传 `true` 时模型无该工具，易表现为「无法联网」。
- **web_search 实现**：`web-search.tool.ts` 改为 `tool()` + Zod `query` 入参，工具描述更利于 function calling 与 Qwen/兼容网关。
- **`build-agent-tools.ts`**：工具列表类型改为 `ClientTool[]`；仍仅在 `enableWebSearch` 为真时注册 `web_search`。
- **`ai.service`（流式/同步元数据）**：`isWebSearchRequestEnabled` 显式解析布尔与常见字符串，避免无 `ValidationPipe` 时误解析。
- **系统提示**：`basic-qa.system.prompt.ts` 在联网开启时强调须先调 `web_search`、不得未调用即声称不能联网。
- **可观测性**：`langgraph.service` 对 `streamBasicQa` 打 debug：是否联网、Tavily 是否配置、工具名列表。
- **API 文档**：`send-chat.dto` 的 `enableWebSearch` 说明默认关、需 `true` 才挂工具与真实出网条件（`TAVILY_API_KEY`）。

## 22:34

### Cursor Prompt

记录「首条发消息再建会话、LLM 起标题」相关后端改造并提交。此前在 `ai.controller` / `stream` 上已加：`conversationId` 可选时建会话、meta 含 id；本批补充 LLM 标题与 Swagger 说明。

### 修改内容总结

- **`LanggraphService.generateConversationTitle`**：用 OpenAI 兼容配置对首条用户消息做短模型调用生成会话标题；失败或无 Key 时返回 `null`。
- **`AiService.resolveAndPersistNewConversationTitle`**：仅当未传 `conversationId` 时落库标题（LLM 或首条截断兜底）；`streamChat` 在发 SSE `meta` 前执行，使 `payload` 可含 `title` 且与库一致；`sendChat` 在事务后同步执行。
- **`ai.controller`（`/ai/chat/stream`）**：OpenAPI 描述与首条无 `conversationId`、新会话与 meta 字段（含 `title`）对齐实际行为。

## 23:50

### Cursor Prompt

将 `docs/long-context.md` 扩为正式设计说明，并「记录这些修改并提交」。

### 修改内容总结

- **`docs/long-context.md`**：长上下文（历史对话过长）的正式设计——目标与原则、异步压缩取舍、目标数据模型（`AiConversationContextSummary` / `AiContextChunk` / `AiContextCompactionJob` 及枚举）、建议类名与主链路/Worker 流程、错误与并发、可观测与未决项；明确与 `threadId`/`seq` 的关系及以 Prisma 迁移为准。
- **`docs/task-log/2026-04-26.md`**：本条确认记录。

## 18:15

### Cursor Prompt

记录这些修改并提交（长上下文：Prisma 表、LangGraph 装配与异步压缩、表 DTO 等）。

### 修改内容总结

- **Prisma 与迁移**：`AiConversationContextSummary`、`AiContextChunk`、`AiContextCompactionJob` 与 `AiContextCompactionJobStatus`；`AiConversation` 关系；迁移 `20260426162806_add_ai_long_context_tables`；`prisma generate` 产物更新。
- **表 DTO**：`ai-conversation-context-summary.dto.ts`、`ai-context-chunk.dto.ts`、`ai-context-compaction-job.dto.ts`。
- **`src/provider/langgraph/`**：`getLongContextConfig`；`long-context/` 下预算与装配、入队、压缩处理；每轮从 DB 组 `messages`、去掉跨请求 `MemorySaver` 作为历史真源；`get_conversation_context` 工具；`streamBasicQa` 入参 `conversationId`；`ai.service` 流式传入；`basic-qa.system.prompt` 补充工具说明；`docs/long-context.md` 与实现同步。
