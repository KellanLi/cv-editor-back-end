# 2026-04-25 Task Log

## 01:37

### Cursor Prompt

为 `Resume` 增加列表/卡片展示用的封面图（`listCoverImageUrl`），并同步 DTO 与 API；之后要求将本次修改写入 task log 并 Git 提交。

### 修改内容总结

- **Prisma / MySQL**：`Resume` 新增可空 `listCoverImageUrl`；迁移 `20260424173501_add_resume_list_cover_image` 对 `Resume` 表加列；已 `pnpm db:generate`，`src/generated` 中 `Resume` 类型已含新字段（勿手改）。
- **DTO**：`ResumeTableDto` 增加 `listCoverImageUrl`；`CreateResumeDto` 可选同名字段；新增 `UpdateResumeListCoverDto`（`id` + `listCoverImageUrl: string | null`），`null` 表清空，自定义 `ValidatorConstraint` 约束取值类型。
- **`resume` 模块**：`create` 支持写入封面 URL；`POST /resume/update-list-cover` 专用于更新/清空；`list` / `detail` 等沿用 Prisma 返回的 `Resume`，自动带出 `listCoverImageUrl`；`updateListCover` 对 `listCoverImageUrl` 为 `undefined` 时 `400` 防漏传。
- 本小节追加 task log，并对上述变更打 Git 提交。
