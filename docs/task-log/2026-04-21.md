# 2026-04-21 Task Log

## 21:18

### Cursor Prompt

将「Task log（确认后变更记录）」skill 写入个人 Cursor 技能库（`~/.cursor/skills/task-log/SKILL.md`）；用户确认后把该次修改记入 task log 并 commit。

### 修改内容总结

- 新增个人技能 `task-log`：`~/.cursor/skills/task-log/SKILL.md`（YAML 元数据 + 中文说明：路径 `docs/task-log/`、`YYYY-MM-DD` 命名、确认后写入、条目模板）。
- 仓库内新增当日任务日志 `docs/task-log/2026-04-21.md` 记录上述工作。

## 21:25

### Cursor Prompt

task-log skill 改为放在项目 skill 即可，不要全局；请修改。

### 修改内容总结

- 在仓库新增 `.cursor/skills/task-log/SKILL.md`（内容与原先全局版一致）。
- 删除全局 `~/.cursor/skills/task-log/` 及空的 `~/.cursor/skills/` 目录。

## 21:27

### Cursor Prompt

请将这些修改记录到 task-log 中并提交。

### 修改内容总结

- 新增 `.cursor/rules/project.mdc`：`alwaysApply` 项目规则（`docs/project.md` 背景与领域名词、Section / Content Template / Info Layer 要点、`src/` 一级目录说明、`generated/` 勿手改）。
- 新增 `.cursor/skills/project-cli-scripts/SKILL.md`：封装 `pnpm g:res`、`pnpm g:pro`、`pnpm run db:migrate -- <name>`、`pnpm db:generate` 的用途与示例。
- 纳入版本库 `docs/project.md`（项目背景与技术栈说明）。
- 本小节为当日 task-log 追加记录；对上述文件执行 Git 提交。

## 21:43

### Cursor Prompt

为 `Content`、`Info` 增加 `order` 字段；同步数据库并 `prisma generate`；将相关改动记入 task log 并提交。

### 修改内容总结

- `prisma/schema.prisma`：`Content`、`Info` 增加非空 `order`（与 `InfoTemplate.order` 一致，用于 Section 内 Content、Content 内 Info 的排序）。
- 新增并应用迁移 `20260421133405_add_content_info_order`（`Content`、`Info` 表增加 `order` 列）；重新生成 `src/generated` Prisma Client。
- `package.json`：`db:migrate` 改为 `node scripts/db-migrate.js`；新增 `scripts/db-migrate.js`，将迁移名正确传给 `pnpm exec prisma migrate dev --name`，避免原先 `pnpm run db:migrate -- <name>` 与 `--name` 拼接导致 Prisma 报错。
- 本小节追加当日 task log；对上述变更执行 Git 提交。

## 23:05

### Cursor Prompt

现在将这些修改记录在 tasklog 中并提交。

### 修改内容总结

- **简历模块**：`POST resume/list|create|delete`，`JwtGuard` + `JwtPayload`；`list` 支持标题模糊与分页；`create` 写入 `title` 与当前用户 `userId`；`delete` 校验归属后事务内按 Info → Content → Section → Resume 级联删除（库中外键为 RESTRICT）。
- **`src/common/dto/business/resume.dto.ts`**：独立标量 DTO，避免与 `Section` 表 DTO 循环引用导致类型退化为 `error`；controller / service 对列表与 Prisma 返回补充显式类型。
- **DTO 命名**：`content-template` 与 `resume` 下请求/列表 DTO 改为带域前缀（如 `CreateContentTemplateDto`、`ListResumeDto`、`ListContentTemplateDataDto` 等），并更新对应 controller、service。
- 本小节追加当日 task log；对 `src/` 上述变更执行 Git 提交（不包含 `.cursor/settings.json`）。
