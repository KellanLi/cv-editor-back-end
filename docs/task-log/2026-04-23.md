# 2026-04-23 Task Log

## 02:00

### Cursor Prompt

给 `Resume` 增加个人信息（证件照 URL、姓名、生日、目标岗位、邮箱、电话，并支持字符串扩展字段）；先改为独立 `ResumeProfile` 表；去掉 `ResumeProfile` 的 `createdAt` / `updatedAt`；再给 `resume` 模块新增「修改简历名称」与「修改简历个人信息」两个接口；最后记录并提交。

### 修改内容总结

- **Prisma schema**：`Resume` 新增与 `ResumeProfile` 的 1:1 可选关系；新增 `ResumeProfile` 模型（`resumeId @unique`、`photoUrl` / `fullName` / `birthDate(@db.Date)` / `targetPosition` / `email` / `phone` / `profileExtra(Json?)`，`onDelete: Cascade`），不含 `createdAt` / `updatedAt`。
- **迁移**（按顺序）：
  - `20260422163657_add_resume_profile_fields`：先把字段加到 `Resume` 上（中间态）。
  - `20260422164547_extract_resume_profile`：手工编辑迁移 SQL——先建 `ResumeProfile` 与外键，再用 `INSERT … SELECT` 从 `Resume` 回填历史数据，最后 `DROP` `Resume` 上的旧列；避免数据丢失。
  - `20260422165103_remove_resume_profile_timestamps`：去掉 `ResumeProfile.createdAt` / `updatedAt`。
  - 每步执行 `pnpm db:generate`，`src/generated` 已随之更新（`ResumeProfile.ts` 等）。
- **DTO**：
  - 新增 `src/common/dto/table/resume-profile.dto.ts`，`ResumeProfileTableDto`（`profileExtra` 用 `unknown`，与 `Info.values` 一致）。
  - `ResumeTableDto` 增加可选 `profile?: ResumeProfileTableDto`；业务层 `ResumeDto` 不用改即可随之暴露 `profile`。
- **`resume` 模块新增两个接口**：
  - `POST /resume/update-title`：`UpdateResumeTitleDto { id, title }`，仅更新 `Resume.title`，严格用 `where: { id, userId }` 隔离用户；返回含 `profile` 的 `Resume`。
  - `POST /resume/update-profile`：`UpdateResumeProfileDto`（id 为简历 ID，其余字段可选；`null` 表示清空，`profileExtra` 的 `null` 转 `Prisma.JsonNull`）。校验归属后在 `$transaction` 里 `resumeProfile.upsert` + `resume.update` 刷 `updatedAt`；最终返回含 `profile` 的 `Resume`；无任何变更字段时抛 `400`。
  - `resume.service.ts`：`list` / `create` / `delete` 对 `Resume` 查询都 `include: { profile: true }`；`create` 时 `profile: { create: {} }`，保证每份简历有一条空 profile。
- **类型/Lint 修复**：`updateProfile` 的写入对象用**显式标量类型**（避免 `Prisma.ResumeProfileUpdateInput` 的算子类型与 `create` 的 `CreateInput` 不兼容）；交互式事务回调显式标注 `Prisma.TransactionClient`，并对误报的 `tx.resumeProfile.upsert` 行加单行 `eslint-disable-next-line @typescript-eslint/no-unsafe-member-access`。
- 本小节追加当日 task log；对上述变更执行 Git 提交。

## 12:18

### Cursor Prompt

给 `src/modules/resume/` 添加一个 `detail` 接口，用来获取表 `Resume` 和 `ResumeProfile` 的信息。

### 修改内容总结

- **DTO**：新增 `src/modules/resume/dto/detail.dto.ts` 的 `DetailResumeDto`（仅含简历 `id`，`@IsNumber`）。
- **Service**：`resume.service.ts` 新增 `detail(params, jwt)`，`findFirst` 按 `id + userId` 校验归属，`include: { profile: true }` 同时拉取 `Resume` 与 `ResumeProfile`；不存在时抛 `NotFoundException('简历不存在')`，与其他方法一致。
- **Controller**：`resume.controller.ts` 新增 `POST /resume/detail`，走 `JwtGuard`，`@ApiResponseWrapper(ResumeDto)` 复用业务 DTO（`ResumeDto` 继承自 `ResumeTableDto`，已携带可选 `profile` 字段，无需新建响应 DTO）。
- 本小节追加当日 task log；对上述变更执行 Git 提交。

## 19:36

### Cursor Prompt

给 `ResumeProfile` 加一个性别字段，同步至数据库与接口；联调时出现 `Unknown argument gender` 报错，确认迁移无误后将数据库同步操作沉淀为 skill。

### 修改内容总结

- **Prisma schema**：`ResumeProfile` 新增 `gender String?`（位于 `birthDate` 与 `targetPosition` 之间），语义与 `User.gender` 一致，应用层约定 `male` / `female` / `other`。
- **迁移**：新增 `prisma/migrations/20260423043300_add_resume_profile_gender/migration.sql`（`ALTER TABLE ResumeProfile ADD COLUMN gender VARCHAR(191) NULL`），已应用到 `resume_system_dev`。
- **DTO**：
  - `src/common/dto/table/resume-profile.dto.ts`（响应 table DTO）新增可空 `gender?: string | null`。
  - `src/modules/resume/dto/update-profile.dto.ts`（入参 DTO）新增可选 `gender?: string`，遵循模块「不传=不改 / `null`=清空」约定。
- **Service**：`resume.service.ts` 的 `updateProfile` 显式 `data` 字面量类型补齐 `gender?: string | null`，否则 `...rest` 展开的新字段会被字面量类型挡掉。
- **排障**：运行时 `PrismaClientValidationError: Unknown argument gender` 根因是 Prisma v7 下 `migrate dev` 未自动执行 `generate`，`src/generated/models/ResumeProfile.ts` 中没有 `gender`；手动执行 `pnpm db:generate` 后问题解除，并提醒重启 `pnpm dev`。
- **Skill 沉淀**：
  - 新增 `.cursor/skills/db-sync/SKILL.md`，强制顺序「改 schema → `db:migrate` → **必须** `db:generate` → `rg` 校验 `src/generated` → 重启 `pnpm dev`」，并列出常见错误表与禁止事项（不要手改 `src/generated`、不要用 `db push` 绕迁移、不要 `as any` 屏蔽未 generate 的 TS 报错）。
  - `.cursor/skills/project-cli-scripts/SKILL.md` 修正误导性表述「`migrate dev` 通常会 generate」，改为指向 `db-sync` 的强约束流程。
- 本小节追加当日 task log；对上述变更执行 Git 提交。

## 22:45

### Cursor Prompt

给 `Section` 表加 `order` 字段表示顺序；原计划新建 module，但确认 `src/modules/section/` 已存在并已在 `app.module.ts` 注册，改为在既有 module 上打通 `order` 字段。`order` 由客户端显式传入，**不**走服务端自动分配。

### 修改内容总结

- **Prisma schema**：`Section` 新增 `order Int @default(0)`（默认 0 仅为迁移时回填历史行，新建走入参）。
- **迁移**：`prisma/migrations/20260423144129_add_section_order/migration.sql`（`ALTER TABLE Section ADD COLUMN order INTEGER NOT NULL DEFAULT 0`），`pnpm db:migrate add_section_order` + `pnpm db:generate`，`src/generated/models/Section.ts` 已含 `order: number`。
- **Table DTO**：`src/common/dto/table/section.dto.ts` 补 `order: number` 与 `@ApiProperty`；`business/SectionDto` 继承自 `SectionTableDto`（`OmitType`），自动带上 `order`，无需改动。
- **模块 DTO**：`src/modules/section/dto/create.dto.ts` 的 `CreateSectionDto` 增加必填 `order: number`（`@IsNumber()`），与既有 DTO 分层规范一致。
- **Service**：`section.service.ts` 的 `list` 排序改为 `orderBy: [{ order: 'asc' }, { id: 'asc' }]`；`create` 直接使用入参 `order` 写入，不再在事务里查 `max(order) + 1`。
- 本小节追加当日 task log；仅提交本次 Section `order` 相关改动（`resume/update-profile.dto.ts`、`ResumeProfile` 相关等无关改动保持在工作区）。
