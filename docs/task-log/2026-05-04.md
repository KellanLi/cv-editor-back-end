# 2026-05-04 Task Log

## 确认后

### Cursor Prompt

在简历维度增加 JD 存储能力，完成从数据库到接口的实现；随后要求记录修改并提交。

### 修改内容总结

- `Resume` 增加 `jobDescriptionText`（列 `job_description_text`，LONGTEXT）及迁移 `add_resume_job_description_text`。
- 新增 `POST resume/update-job-description`；`resume/create` 可选带入 JD；列表/详情 DTO 暴露该字段。
- AI：`buildBasicQaSystemPrompt` 优先读简历字段，空则回退 `AiGlobalContext` key=`job_description`；`load_resume_context` 输出含【JD】段。
- SSE 增加触发类型 `resume.update-job-description`；`scripts/db-migrate.js` 修正 `pnpm db:migrate -- <name>` 传参。
- Prisma Client（`src/generated`）随 schema 更新。
