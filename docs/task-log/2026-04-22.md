# 2026-04-22 Task Log

## 00:53

### Cursor Prompt

总结 `src/common/dto/business/` 中的修改，先写入 task log，再提交。

### 修改内容总结

- **新增** `content.dto.ts`、`info.dto.ts`、`section.dto.ts`：从对应 `table` DTO 用 `OmitType` 去掉反向 relation（`content`、`section` 等），`ContentDto` / `SectionDto` 将嵌套子项改为业务型 `InfoDto[]` / `ContentDto[]`，便于 Swagger 与 `@ApiResponseWrapper` 文档一致。
- **`resume.dto.ts`**：改为继承 `OmitType(ResumeTableDto, ['user', 'sections'])`，并显式声明可选 `sections?: SectionDto[]`；修正类注释与实现一致（不含 `user`，可含业务型 `sections`）。
- **`content-template.dto.ts`**：表 DTO 导入统一为 `ContentTemplateTableDto`；在去掉表型 `infoTemplates` / `user` 后，重新声明 `infoTemplates?: InfoTemplateDto[]`。
- **`info-template.dto.ts`**：表 DTO 导入更名为 `InfoTemplateTableDto`；`OmitType` 仅省略 `contentTemplate`（保留 `id`、`contentTemplateId` 等标量/外键字段）。

## 01:05

### Cursor Prompt

（续）总结 `business` 修改写入 task log 并提交后，补充记录与构建修复。

### 修改内容总结

- **`resume.dto.ts`**：类注释改为与实现一致（可含可选 `sections`，不含 `user`）。
- **`content-template.service.ts`**：`list` 返回的 `list` 对 Prisma 查询结果使用 `as unknown as ContentTemplateDto[]`，避免 `InfoTemplate.names`（DB `Json`）与 DTO 中 `string[]` 在 `tsc` 下的不兼容；`list` 仍声明为 `Promise<ListContentTemplateDataDto>`。

## 01:40

### Cursor Prompt

请将现在所有的修改记录到 task log 中，并提交。

### 修改内容总结

- **Section 模块**：新增 `src/modules/section`（`list` / `create` / `delete` / `update-content`），`AppModule` 注册 `SectionModule`；校验简历与内容模板归属用户，`create` 按 `InfoTemplate` 生成默认 `Content`/`Info`，`delete` 与 `update-content` 级联清理后写回。
- **table DTO**：`ContentTemplateDtoTable` 更名为 `ContentTemplateTableDto` 并全局引用对齐；`ContentTableDto` 补充 `order`、可选嵌套 `section`；`InfoTableDto` 补充 `order`，`values` 改为 `unknown`（JSON），`content` 的 `@ValidateNested` 去掉误用的 `{ each: true }`；`SectionTableDto.resume` 改为可选；删除若干 Prisma 模型注释块。
- **content-template**：Swagger `@ApiTags` / `@ApiOperation` 文案由「模块」改为「内容模板」；`list` 依赖返回类型推断，移除 `list` 的显式 cast 与冗余类型导入。
- **resume**：Controller 去掉显式 `Promise<…>` 返回注解与未使用的 `Resume` 类型导入；Service 去掉显式返回类型与未使用的 `ListResumeDataDto` 导入。
- **Cursor**：新增 `.cursor/skills/nest-module/SKILL.md`（Nest 业务模块约定）；新增 `.cursor/settings.json`（启用 Prisma 插件）。

## 04:28

### Cursor Prompt

记录这些修改并提交（含此前 storage、DTO skill、前端上传示例等相关变更）。

### 修改内容总结

- **图片存储模块**：`pnpm g:res storage` 生成后实现本地磁盘存储；`POST /storage/upload`（JWT + multipart `file`）、`GET /storage/file/:storedName`；`UploadStorageDataDto` 置于 `src/modules/storage/dto/upload.dto.ts`；`MemoryUploadedImageFile` 在 `src/types/storage.types.ts`。
- **配置**：`src/config/loader.ts` 增加 `api.globalPrefix`、`storage.root`；`AppModule` 的 `ConfigModule.forRoot` 增加 `load: [configLoader]`。
- **全局**：`TransformInterceptor` 对 `StreamableFile` 直通不包统一 JSON；`.gitignore` 忽略 `/uploads`。
- **文档与约定**：新增 `.cursor/skills/dto-layering/SKILL.md`（DTO 分层决策）；更新 `nest-module` skill 与 DTO 表格/目录说明一致；新增 `docs/frontend-storage-upload.ts`（前端 `fetch` 上传示例）。
