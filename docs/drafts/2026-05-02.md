# 2026-05-02

第一篇草稿。草稿主要用来：1. 整理思路 2. 列每次work的计划/目标

项目进度是怎样的？

目前应该是Ask/Agent模式已经跑通了，但是Agent模式的可用性不高，存在幻觉，以及工具调用问题。解决思路是：编写测试用例测试并收集问题，然后一个一个解决，目前优先级不是最高的。

今天完成AI诊断简历的功能。

AI简历诊断SOP设计：

1. **JD获取/生成**

从数据库获取用户给简历提供的JD。若没有，则通过用户的目标岗位联网查询并生成JD。

2. **简历内容预处理**

直接从数据库获取的简历内容比较差，如下：

```

```

LLM对其的理解操作很困难，对其做预处理。

3. **通过JD + 简历内容生成标准简历编写思路**

这个编写思路既要理解用户的意图，同时更要有所改进。

4. **JD + 简历编写思路生成评价维度**

5. **对各个维度进行打分评价，并生成总体评价**
6. **给出每一部分的修改意见 + 整体修改意见**

对于每个Section的每个Content，修改可以有以下操作：

- 删除：与应聘岗位关系不大
- 扩充：内容很有必要，且应该多写，则可以对齐进行扩充补全
- 简练：内容有必要，但是用户写的较为冗余，则需要对内容摘取重要的部分并进行简练

对于整体，修改仅有一个操作：增加。

- 简历整体维度：如果简历缺少某个必要的部分（如应聘后台开发时，同时缺少项目经历和实习经历），则应当提示对简历内容进行必要的增添
- Section维度：如过某个重要的Section内容过少（如应聘后台开发时，没有实习经历，仅有一个项目经历），则应当提示对该Section进行增加的操作
